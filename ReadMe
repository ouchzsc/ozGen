CSV to Lua 配置生成工具

使用范例：
csv配置目录：
    可以有多个子目录，每个子目录看做是一个逻辑模块，一个子目录中包含一个xml文件和多个csv文件。
    csv文件：数据源，方便策划用Excel进行批量编辑。其中以##开头，或者第一格为空的行，都是无效行。
    xml文件：数据定义。申明每一列的变量名，变量类型，表格引用。
使用工具生成后会生成csv对应的lua配置。

限制：
    xml描述文件和文件夹名字必须一致。


不支持Bean
    list支持ref，不支持list Bean，原因：表格难读,列数变长。
    bean生成的类并不能自定义成员函数，只能用来做类型限定。
    没有Bean也可以不拆表。头条和快手有两个项目就没有支持Bean，使用起来并没有觉得不自然。面向列进行抽象灵活程度更高。
    面向Bean的抽象牺牲了一部分灵活性，相当于继承的表达能力，面向列相当于组合。让程序员多写点组合代码是ok的，组合代码很容易写对。

list：
    以逗号为分隔符

支持 config，ref，enum，主键
多态和索引键暂不支持。
